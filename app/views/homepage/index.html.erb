<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Best Patcharapon | Test Engineer & Developer Portfolio</title>
  <%= csrf_meta_tags %>

  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

  <!-- Styles -->
  <link rel="stylesheet" href="/homepage.css">
  <link rel="stylesheet" href="/chatbot.css">
</head>
<body>
  <div class="bento-container">
    <!-- Header Navigation -->
    <%= render "homepage/partials/header" %>

    <!-- Main Content -->
    <div class="main-content">
      <!-- Profile Section -->
      <section class="profile-info-section" id="about">
        <div class="profile-grid">
          <!-- Profile Image Stack -->
          <%= render "homepage/partials/profile_stack" %>

          <!-- Profile Information -->
          <%= render "homepage/partials/profile_info" %>
        </div>
      </section>

      <!-- Skills Section -->
      <%= render "homepage/partials/skills_section" %>

      <!-- Projects Section -->
      <%= render "homepage/partials/projects_section" %>

      <!-- Experience Section -->
      <%= render "homepage/partials/experience_section" %>
    </div>
  </div>

  <!-- Chatbot Widget -->
  <%= render "homepage/partials/chatbot" %>

  <!-- Image Lightbox Modal -->
  <div id="image-lightbox" class="lightbox">
    <div class="lightbox-container">
      <span class="lightbox-close">&times;</span>
      <img src="" alt="" id="lightbox-image" class="lightbox-image" />
      <p id="lightbox-title" class="lightbox-title"></p>
    </div>
  </div>

  <!-- Pass data to JavaScript -->
  <script>
    window.TYPING_TEXTS = <%= raw typing_texts.to_json %>;
  </script>

  <!-- External JavaScript -->
  <script src="/homepage.js"></script>
  <script src="/chatbot.js"></script>
</body>
</html>
